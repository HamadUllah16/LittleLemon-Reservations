import React from 'react'

function MainButton({buttonName}) {
  return (
    <>
      <button className='mainButton primaryColor2 highlightText primaryText'>{buttonName}</button>
    </>
  )
}

export default MainButton
